<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangx.oj.mapper.SubmissionMapper">
    <select id="findSubmissionPagination" resultType="com.wangx.oj.entity.Submission">
        select * from t_submission limit #{start},#{end}
    </select>
</mapper>